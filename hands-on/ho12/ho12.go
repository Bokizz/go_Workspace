package main

import "fmt"

func main() {
	fmt.Println("This code needs to be runned through the terminal(bash), yes that is the exercise :/")
	fmt.Println("And also we need to build it for the three operating systems:Windows,Mac,Linux")
}

// komandata za da se napravi eden fajl izrabotliv na eden operativ sistem e GOOS=x go build; x e imeto na operativniot sistem i gi ima vrednostite windows,darwin(za mac) i linux
// kako rezultat se dobiva exe fajl so imeto na folderot koe e executable file od kodot koj go imame i mozhe da se prati za da go koristi korisnikot od drug operativen sistem
